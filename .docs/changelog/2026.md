# Changelog 2026

## [2026-08-08] — Reconstrucción editorial de la landing pública / ADR-016

**Tipo:** DECISIÓN + IMPLEMENTACIÓN
**Área:** decisions/ | architecture/
**Archivos afectados:**
- `.docs/decisions/ADR-016-reconstruccion-editorial-landing.md` (CREACIÓN)
- `.docs/decisions/ADR-015-identidad-dark-luxury-landing.md` (ENMIENDA: nota de que la composición de secciones se sustituye por la editorial del ADR-016; colores de marca intactos)
- `.docs/decisions/ADR-013-personalizacion-landing-publica.md` (ENMIENDA: hero consume tagline/heroTitle/heroSubtitle/heroImageUrl como override; nuevos defaults de heroTitle/heroSubtitle)
- `.docs/architecture/modules.md` (MODIFICACIÓN: roles editoriales de las componentes `landing/`, sección unificada Horarios/Ubicación, Gallery/Stats como capas preparadas)

**Descripción:**
Se documentó e implementó la **reconstrucción editorial / premium / urbana / cinematográfica** de la landing pública `/[slug]`, que reemplaza el aspecto "dashboard" de la composición por tarjetas del ADR-015. **Composición editorial de 8 secciones** (HERO → INTRO → SERVICIOS lista numerada con hairline → EQUIPO lista editorial → EXPERIENCIA → HORARIOS+UBICACIÓN unificados → CTA FINAL → FOOTER), **sin grillas de tarjetas ni cajas `surface`**, con uso moderado del dorado (solo CTA primarios, kickers, hairlines). El **HERO** pasa a gobernarse por **defaults globales + override por tenant** reutilizando los campos existentes de ADR-013 (`tagline`/`heroTitle`/`heroSubtitle`/`heroImageUrl`), con nuevos defaults `heroTitle` **«EL CORTE QUE TE DEFINE.»** y `heroSubtitle`; **no se añade campo nuevo** al esquema. La **identidad ADR-015 permanece intacta** (paleta negro/marfil/old-gold y fuentes Marcellus/Spectral/IBM Plex Mono). 9 archivos de código modificados: landing-config.ts, types/landing.ts, LandingPage/Hero/Sections/CTA/Footer/Nav y globals.css.

**Motivación:**
La implementación previa basada en ADR-015 se percibía como **dashboard/CRUD** (grid de tarjetas grises + dorado masivo). El problema era estructural, no cromático: se reconstruye la composición hacia una estética editorial sin migrar esquema (0 campos nuevos), sin tocar lógica de reservas ni contratos públicos.

---

## [2026-08-08] — Nueva identidad dark luxury de la landing / ADR-015

**Tipo:** DECISIÓN (documentada antes de implementar)
**Área:** decisions/ | architecture/ | requirements/
**Archivos afectados:**
- `.docs/decisions/ADR-015-identidad-dark-luxury-landing.md` (CREACIÓN)
- `.docs/decisions/ADR-013-personalizacion-landing-publica.md` (ENMIENDA: defaults de paleta dark luxury; esquema intacto)
- `.docs/decisions/ADR-014-identidad-visual-landing.md` (ENMIENDA: identidad reemplazada por ADR-015 como default)
- `.docs/architecture/modules.md` (MODIFICACIÓN: componentes `landing/` actualizados: LandingHero/Nav/Sections + LandingFooter/Gallery/Stats)
- `.docs/requirements/mvp-scope.md` (MODIFICACIÓN: línea de fuentes actualizada)

**Descripción:**
Se documentó la nueva **identidad dark luxury / barbershop premium** para la landing pública `/[slug]` que reemplazará a «Umbral de tinta + libro de cuentas» (ADR-014) como default. **Paleta** (mismos 6 tokens ADR-013, esquema sin migración): bg `#0A0A0A`, surface `#111111`, fg marfil `#F2EDE4`, muted taupe `#8A8178`, acento dorado old-gold `#C9A227`, danger `#C0392B`. **Tipografía**: se MANTIENEN Marcellus/Spectral/IBM Plex Mono. Hero con imagen velada + doble CTA (dorado + outline), navbar transparente→oscuro con hamburguesa mobile, tarjetas premium con hairline dorado, barber-pole → hilo/motivo dorado, y se preparan **Galería/Stats como capas frontend condicionales** (ocultas por defecto, sin inventar datos ni campos nuevos). El mecanismo de personalización ADR-013 (6 tokens, CSS vars scoped, merge, "Restaurar default") permanece intacto.

**Motivación:**
El programador aprobó la **Opción A** (documentar antes de implementar): fijar la nueva identidad dark luxury en un ADR-015 para que Planner/Executor implementen más tarde sin migrar el esquema ADR-013 ni tocar la lógica de reservas (BookingWizard).

---

## [2026-08-07] — Identidad visual «Umbral de tinta + libro de cuentas» / ADR-014

**Tipo:** DECISIÓN + IMPLEMENTACIÓN
**Área:** decisions/ | architecture/
**Archivos afectados:**
- `.docs/decisions/ADR-014-identidad-visual-landing.md` (CREACIÓN)
- `.docs/decisions/ADR-012-landing-publica-reservas.md` (ENMIENDA: separación del formulario a `/[slug]/reservar`)
- `.docs/decisions/ADR-013-personalizacion-landing-publica.md` (ENMIENDA: nuevos defaults de paleta/tipografía)
- `.docs/architecture/modules.md` (MODIFICACIÓN: ruta `[slug]/reservar` + componentes `landing/`)

**Descripción:**
Se implementó y documentó la nueva identidad visual de la landing pública `/[slug]` (paleta marfil/tan/tinta/oxblood; fuentes Marcellus/Spectral/IBM Plex Mono; barber-pole como firma visual; animaciones gated por `prefers-reduced-motion`). El formulario de reserva se separa a la ruta propia `/[slug]/reservar` reutilizando `BookingWizard`. Se enmienda ADR-012/013 con la referencia cruzada y la física ADR-013 (CSS vars + merge de config) queda intacta.

**Motivación:**
Cerrar el trabajo pendiente de documentación del ADR-014: registrar la decisión de arquitectura y la implementación ya commiteada en `ec78254` (22 archivos), y mantener `.docs/architecture/modules.md` como fuente de verdad actualizada del paralelo estructural del frontend.

---

## [2026-08-05] — Panel admin de personalización de la landing pública por tenant / ADR-013

**Tipo:** CREACIÓN / MODIFICACIÓN
**Área:** decisions/ | architecture/ | requirements/ | api/
**Archivos afectados:**
- `.docs/decisions/ADR-013-personalizacion-landing-publica.md` (CREACIÓN)
- `.docs/architecture/modules.md` (MODIFICACIÓN: módulo backend `landing/` + panel frontend `/admin/landing`, diagrama de dependencias, contratos HTTP de `/v1/landing`)
- `.docs/requirements/mvp-scope.md` (MODIFICACIÓN: sección "Personalización de la landing pública por tenant" como parte del alcance MVP)

**Descripción:**
Se documentó el trabajo sin commitear que añade un **panel admin de personalización de la landing pública por tenant** (módulo backend `landing` + panel frontend `/admin/landing`). ADR-013 decide: config persistida en `Tenant.settings.landing` (JSONB) fusionada sobre `LANDING_DEFAULTS` con merge profundo defensivo (`mergeLandingConfig`); endpoints `/v1/landing` GET/PUT protegidos con JWT + RBAC (rol admin) y escopados por `tenantId` del `@CurrentUser`; la config aplica SOLO a la landing pública `/[slug]` (aislamiento por CSS variables de scope local; los dashboards conservan su tema); inyección pública vía `PublicShop.landing`; fuentes Archivo/Space Grotesk cargadas con `next/font` solo para la landing. Los contratos de API de `landing` se documentan dentro del ADR-013 (el repo no mantiene contratos por módulo en `.docs/api/`, solo `versioning.md`), sin crear un documento huérfano.

**Motivación:**
La documentación existente (ADR-012) solo cubría la landing pública de reservas, no el panel de personalización. Se registra la decisión de arquitectura y se mantiene el alcance del MVP alineado con el trabajo ya implementado.

---

## [2026-08-04] — Landing pública de reservas + break del barbero / ADR-011 + ADR-012

**Tipo:** DECISIÓN
**Área:** decisions/ | architecture/ | requirements/
**Archivos afectados:**
- `.docs/decisions/ADR-011-break-horario-barbero.md` (CREACIÓN)
- `.docs/decisions/ADR-012-landing-publica-reservas.md` (CREACIÓN)
- `.docs/architecture/modules.md` (MODIFICACIÓN: módulo `public` + `schedule`, diagrama de dependencias, sección de contratos HTTP y cálculo de disponibilidad)
- `.docs/requirements/mvp-scope.md` (MODIFICACIÓN: break + landing pública como parte del alcance; v2.2 ajustado)

**Descripción:**
Se documentó la arquitectura de la **landing pública de reservas por barbería** (self-service sin registro) y el **break del barbero** necesario para el cálculo de disponibilidad. ADR-012 define endpoints públicos sin JWT bajo `/v1/public/:slug` (metadatos, disponibilidad, lookup de cliente por email, creación de reservas), el flujo frontend en 4 pasos, el cálculo de disponibilidad real y el requisito admin de **slug auto-generado + editable** (slugify + sufijo único en colisión). ADR-011 decide añadir `Schedule.breakStartTime`/`breakEndTime` (un solo bloque por día, migración de DB requerida) como alternativa a una tabla separada o reutilizar `AvailabilityBlock`.

**Motivación:**
Incluir self-service de citas en el MVP de la landing y documentar las decisiones de modelo de datos (break) y arquitectura (endpoints públicos) antes de implementar.

---

## [2026-08-04] — Despliegue en producción (Vercel + Render + Neon + Upstash) / ADR-010

**Tipo:** DECISIÓN + INFRAESTRUCTURA
**Área:** decisions/ | architecture/
**Archivos afectados:**
- `.docs/decisions/ADR-010-arquitectura-de-despliegue.md` (CREACIÓN)
- `.docs/architecture/deployment.md` (CREACIÓN)
- `render.yaml` (CREACIÓN — Blueprint de Render)
- `frontend/vercel.json` (CREACIÓN — config de Vercel)

**Descripción:**
Se desplegó TrimFlow en producción con servicios independientes por capa: frontend en **Vercel** (`frontend/`), backend en **Render** (`backend/`, build nativo de Node 20), base de datos en **Neon** y Redis en **Upstash**. Se corrigieron problemas de deploy: `nest: not found` (Render omite devDependencies → `npm ci --include=dev`), migraciones que no se ejecutaban (`migrationsRun: true` en TypeOrmModule) y seed que no poblaba la base (seed automático si `users` está vacía). El prefijo de API es `/v1`.

**Motivación:**
Poner el stack completo en producción y documentar la arquitectura de despliegue para reproducibilidad.

---

## [2026-08-02] — Riesgo aceptado de dependencias transitivas (postcss + sharp) / ADR-009

**Tipo:** DECISIÓN
**Área:** decisions/ | reports/
**Archivos afectados:**
- `.docs/decisions/ADR-009-gestion-de-riesgo-dependencias-transitivas.md` (CREACIÓN)
- `reports/2026-08-02-frontend-audit.md` (CREACIÓN)

**Descripción:**
`npm audit` reportaba 4 vulnerabilidades high en el frontend. `npm audit fix` (sin `--force`) resolvió `brace-expansion` (1 de 4). Las 3 restantes (`postcss` y `sharp`, transitivas de `next`) quedaron documentadas como **riesgo aceptado** en el ADR-009. Se descartó `npm audit fix --force` (instalaría next@9.3.3, downgrade breaking) y se decidió NO actualizar Next.js (el rango vulnerable incluye la última estable 16.2.12). Criterio de cierre: cuando exista `next` estable > 16.3.0-preview.7 sin las dependencias vulnerables.

**Motivación:**
Resolver las observaciones BAJA del sprint de testing del frontend manteniendo `test`, `lint` y `build` verdes y sin introducir breaking changes.

## [2026-08-01] — ADR-007: rediseño de UI de dashboards (inspirado en Admindek Next)

**Tipo:** CREACIÓN / MODIFICACIÓN
**Área:** decisions/ | PROJECT.md
**Archivos afectados:**
- `.docs/decisions/ADR-007-rediseno-ui-dashboards.md` (CREACIÓN)
- `.docs/PROJECT.md` (MODIFICACIÓN: tabla de stack frontend)

**Descripción:**
Se documentó la adopción del rediseño de UI "inspirado en Admindek Next" para los 3 dashboards (admin, barber, super-admin). El ADR-007 fija la paleta ocean-blue (#4680ff) y tokens del template aplicados a shadcn/ui, dark mode con toggle vía next-themes, y la incorporación de recharts como nueva dependencia para los charts (con restricción de licencia: réplica inspirada, componentes propios desde cero). Además se corrigió la tabla de stack del frontend en PROJECT.md a las versiones reales (Next.js 16.2.x, React 19.x, Tailwind CSS 4.x, lucide-react) y se añadió recharts como dependencia prevista.

**Motivación:**
El stack real del frontend (verificado en frontend/package.json) estaba desactualizado en PROJECT.md (declaraba React 18.x y Tailwind 3.x), y faltaban ADRs sobre decisiones de UI. Se requiere una fuente de verdad para el objetivo de rediseño de dashboards, incluyendo tokens de tema, dark mode, charts y el límite de licencia.

## [2026-07-28] — ADR-001 aceptado: repos separados

**Tipo:** DECISIÓN
**Área:** decisions/
**Archivos afectados:**
- `.docs/decisions/ADR-001-mono-repo-o-repos-separados.md`

**Descripción:**
Se aceptó la decisión arquitectónica de usar repositorios separados (backend y frontend como carpetas hermanas) sin herramienta de monorepo. La decisión prioriza simplicidad e independencia sobre código compartido.

**Motivación:**
El programador determinó que cada proyecto debe tener su propio repo, tooling y pipeline. No se usará Nx/Turborepo.

## [2026-07-28] — Actualización Next.js 14.x → 16.2.x

**Tipo:** ACTUALIZACIÓN
**Área:** frontend stack
**Archivos afectados:**
- `.docs/PROJECT.md`

**Descripción:**
Se actualizó la versión de Next.js en la tabla de Technology Stack / Frontend de 14.x a 16.2.x para reflejar el stack real del frontend.

**Motivación:**
El proyecto utiliza Next.js 16.2.x como framework React. La documentación estaba desactualizada.

## [2026-07-27] — Inicialización de la documentación arquitectónica

**Tipo:** CREACIÓN
**Área:** architecture/ | decisions/ | database/ | api/ | requirements/ | changelog/
**Archivos afectados:**
- `.docs/changelog/2026.md`
- `.docs/architecture/modules.md`
- `.docs/architecture/testing-strategy.md`
- `.docs/architecture/observability.md`
- `.docs/architecture/configuration.md`
- `.docs/requirements/mvp-scope.md`
- `.docs/database/migrations.md`
- `.docs/api/versioning.md`
- `.docs/decisions/ADR-001-mono-repo-o-repos-separados.md`
- `.docs/decisions/ADR-002-estrategia-de-pruebas.md`
- `.docs/decisions/ADR-003-versionado-de-api.md`
- `.docs/decisions/ADR-004-gestion-de-configuracion.md`
- `.docs/decisions/ADR-005-observabilidad.md`
- `.docs/decisions/ADR-006-migraciones-de-base-de-datos.md`

**Descripción:**
Creación completa de la estructura de documentación arquitectónica del proyecto TrimFlow. Se generaron ADRs, documentos de arquitectura, alcance de MVP, estrategias de pruebas, migraciones, observabilidad y configuración.

**Motivación:**
Inicialización del sistema de documentación del proyecto. Todo agente (Orquestador, Planner, Executor, Auditor) necesita estas fuentes de verdad para operar consistentemente.

## [2026-07-27] — Reportes FINALES de componentes de infraestructura

**Tipo:** CIERRE
**Área:** reports/
**Archivos afectados:**
- `reports/FINAL_2026-07-27_global-exception-filter.md`
- `reports/FINAL_2026-07-27_guards-decorators.md`
- `reports/FINAL_2026-07-27_auth-module.md`

**Descripción:**
Se generaron los reportes técnicos finales de 3 componentes que habían sido aprobados por el Agente Auditor en iteraciones previas pero no tenían reporte FINAL. Cierre completo de la capa de infraestructura compartida.

**Componentes cerrados:**
- Global Exception Filter + 7 excepciones de dominio (3 iteraciones)
- Guards (JwtAuth, Roles) + Decorators (@CurrentUser, @Roles, @Tenant) (5 iteraciones)
- Auth Module (login JWT, refresh token, estrategia) (7 iteraciones)

## [2026-07-27] — Implementación completa de módulos de dominio

**Tipo:** IMPLEMENTACIÓN
**Área:** src/modules/
**Iteraciones:** 8 submódulos, 1 ciclo cada uno (APROBADO)

**Descripción:**
Implementación completa de los 8 módulos de dominio del backend TrimFlow siguiendo la jerarquía definida en modules.md y el alcance del MVP.

**Módulos implementados:**
| Módulo | Entidad | Servicio | Controlador | DTOs | Interfaces | Veredicto |
|--------|---------|----------|-------------|------|------------|-----------|
| Tenants | CRUD + soft-delete + settings JSONB | ITenantService + logging + validate slug | v1/tenants + activate/suspend | ✅ | ✅ | APROBADO |
| Branches | CRUD + soft-delete + FK Tenant | IBranchService + logging + findByTenant | v1/branches | ✅ | ✅ | APROBADO |
| Barbers | CRUD + soft-delete + FK Branch | IBarberService + logging + findByBranch | v1/barbers | ✅ | ✅ | APROBADO |
| Services | CRUD + soft-delete + FK Branch | IServiceService + logging + findByBranch | v1/services | ✅ | ✅ | APROBADO |
| Customers | CRUD + soft-delete + FK Branch | ICustomerService + logging + findByBranch/Email | v1/customers | ✅ | ✅ | APROBADO |
| Appointments | CRUD + soft-delete + FKs (Barber/Customer/Service) | IAppointmentService + double-booking check | v1/appointments + cancel/complete | ✅ | ✅ | APROBADO |
| Notifications | CRUD + soft-delete + status/channel | INotificationService + markSent/markFailed | v1/notifications | ✅ | ✅ | APROBADO |
| Settings | CRUD + soft-delete + key-value por branch | ISettingService + get/set/getAll/delete | v1/settings | ✅ | ✅ | APROBADO |

**Características comunes implementadas en todos los módulos:**
- API versionada bajo `/v1/` (ADR-003)
- Soft-delete con `@DeleteDateColumn()` en todas las entidades
- Guards JWT + RBAC con roles específicos por endpoint
- Logging operacional con TrimflowLoggerService
- Interfaces explícitas (ISP) para todos los servicios
- DTOs de creación/actualización con class-validator
- DTOs de respuesta con documentación Swagger
- Relaciones entre entidades con claves foráneas y CASCADE
- Prevención de doble reserva en Appointments (core del negocio)

## [2026-07-27] — Migraciones, Seed y Auth real

**Tipo:** IMPLEMENTACIÓN
**Área:** database/migrations/ | database/seeds/ | modules/auth/

**Descripción:**
Migraciones de base de datos, seed de datos demo, y reemplazo de MOCK_USERS
por autenticación real contra base de datos.

**Migraciones:**
| Migración | Tablas | Estado |
|-----------|--------|--------|
| `1785210750237-CreateAllTables` | tenants, branches, barbers, services, customers, appointments, notifications, settings | ✅ Ejecutada |
| `1785210911058-AddUsersTable` | users | ✅ Ejecutada |

**Seed demo:**
- 2 tenants (Barbería El Clásico, Studio 42)
- 3 sucursales, 4 barberos, 7 servicios, 5 clientes, 5 citas
- 3 usuarios con passwords hasheados (bcrypt)

**Auth real:**
- Entidad User con TypeORM (UUID, email único, password hasheado, rol, tenantId)
- AuthService usa UserRepository + bcrypt en lugar de MOCK_USERS
- JwtStrategy valúa contra BD
- Seed incluye usuarios para login inmediato

## [2026-07-27] — Rate Limiting

**Tipo:** IMPLEMENTACIÓN
**Área:** src/app.module.ts

**Descripción:**
Instalación y configuración de `@nestjs/throttler` para rate limiting global.

**Configuración:**
- 100 solicitudes por 60 segundos (configurable vía env vars)
- Aplica a todos los endpoints automáticamente via `ThrottlerGuard` global
- Variables `RATE_LIMIT_TTL` y `RATE_LIMIT_MAX` ya existían en config y .env
